import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.luxurywealth.tracker',
  appName: 'Wealth Tracker',
  webDir: 'dist'
};

export default config;
